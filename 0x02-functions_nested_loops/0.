#include <stdio.h>
#include "_putchar.c"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char a [] ="_putchar";
for(int i = 0 ; i<8 ; i++)
_putchar(a[i]);
_putchar('0');
return (0);
}
