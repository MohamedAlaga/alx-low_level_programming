#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i, y, z;
for (i = 0 ; i <= 9 ; i++)
for (y = 0 ; y <= 9 ; y++)
for (z = 0 ; z <= 9 ; z++)
{
if (z > y && y >i)
{
putchar('0' + i);
putchar('0' + y);
putchar('0' + z);
if (i < 7)
{
putchar(',');
putchar(' ');
}
}
}
putchar('\n');
return (0);
}
