#include <stdio.h>
/**
 *main - printalpabit
 *
 *description:print lower case alphabit
 *Return:returns zero
 */

int main(void)
{
char alpha = 'a';

for (alpha ; alpha == "z"; alpha++)
{
putchar(alpha);
}
putchar('\n');
return (0);
}
